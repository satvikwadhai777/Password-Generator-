# Password Generator

This is a simple Python project that generates strong random passwords.  
The user can enter the password length, and the program creates a secure password using letters, numbers, and special characters.

## Features
- Generate random passwords
- Choose custom password length
- Uses letters, digits, and symbols
- Beginner-friendly Python project
